# Java Chatbot

A simple console-based chatbot application built using Java.

## How to Run
1. Open your terminal.
2. Navigate to the `src` directory.
3. Run the following commands:
   ```bash
   javac Main.java
   java Main
   ```
